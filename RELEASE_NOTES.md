# betterNJUVPN 0.1.1

提供 Windows x64 便携 ZIP 和安装包。支持校园账号与短信登录、本地 Web 代理、系统代理设置与恢复、全局虚拟网卡分流，以及本地代理流量显示。

虚拟网卡需要管理员权限；南大网页经本地代理，其余流量直连。与其他正在运行的 TUN 网卡冲突时会提示关闭冲突网卡，避免双层转发拖慢网络。校内 IP 和非 Web 端口不经学校网关。

安装器提供当前用户证书信任选项；首次开启代理时也会提示。卸载时恢复本程序接管的系统代理、移除本程序的证书并清理安装目录中的运行数据。

修复退出后可能留下失效本地代理地址的问题；界面开关按实际监听状态显示。普通网站的 HTTPS 代理转发已通过本地回归测试。

安装包和便携包均不包含账号、密码、会话或 CA 私钥。虚拟网卡使用的 Mihomo 核心按 GPL-3.0 发布，许可证随程序提供。
